---
sidebar_label: Groups
sidebar_position: 1
title: Groups
---

> The Groups  component renders the list of all available groups.<br/>
This list is composed of items containing groups's image, title, info and an enter button, that allows users to be redirected to the main group page.<br/>
If set as an option via props, at the top of the component will appear a search bar that allows the users to filter groups.

[//]: # (import Logged from "../../Logged";)

[//]: # (import {Groups} from '@selfcommunity/react-ui';)

[//]: # ()
[//]: # (<Logged><Groups/></Logged>)

#### Usage

```jsx
 import React from 'react';
 import {SCContextProvider} from '@selfcommunity/react-core';
 import {Groups} from '@selfcommunity/react-ui';

 function App() {
   const conf = {
     portal: '<community-path>',
     session: {
       type: 'OAuth',
       token: '<community-token>',
       refreshToken: '<community-refresh-token>',
       refreshTokenEndpoint: '<community-refresh-endpoint>',
     },
   };
   return (
     <SCContextProvider conf={conf}><Groups /></SCContextProvider>
   );
 }
```







