---
sidebar_label: Category Header
sidebar_position: 1
title: Category Header
---

> The Category Header component renders a category header composed of category's cover, title, slogan and <br/>
 a followers section followed by a button that allows users to follow/unfollow the category.

import CategoryHeader from './categoryHeader.js';
import Logged from "../../Logged";

<Logged><CategoryHeader/></Logged>

#### Usage

```jsx
 import React from 'react';
 import {SCContextProvider} from '@selfcommunity/react-core';
 import {CategoryHeader} from '@selfcommunity/react-ui';

 function App() {
   const conf = {
     portal: '<community-path>',
     session: {
       type: 'OAuth',
       token: '<community-token>',
       refreshToken: '<community-refresh-token>',
       refreshTokenEndpoint: '<community-refresh-endpoint>',
     },
   };
   return (
     <SCContextProvider conf={conf}><CategoryHeader /></SCContextProvider>
   );
 }
```