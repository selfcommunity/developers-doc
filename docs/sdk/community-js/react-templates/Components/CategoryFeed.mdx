---
sidebar_label: Category Feed
sidebar_position: 1
title: Category Feed
---

> The Category Feed template renders the category feed.


import Logged from "../../Logged";
import {CategoryFeed} from '@selfcommunity/react-templates';

<Logged><CategoryFeed categoryId={3}/></Logged>

#### Usage

```jsx
 import React from 'react';
 import {SCContextProvider} from '@selfcommunity/react-core';
 import {CategoryFeed} from '@selfcommunity/react-templates';

 function App() {
   const conf = {
     portal: '<community-path>',
     session: {
       type: 'OAuth',
       token: '<community-token>',
       refreshToken: '<community-refresh-token>',
       refreshTokenEndpoint: '<community-refresh-endpoint>',
     },
   };
   return (
     <SCContextProvider conf={conf}><CategoryFeed/></SCContextProvider>
   );
 }
```
