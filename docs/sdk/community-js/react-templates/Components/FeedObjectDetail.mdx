---
sidebar_label: Feed Object Detail
sidebar_position: 1
title: Feed Object Detail
---

> The Feed Object Detail template renders the feed object detail page.

import FeedObjectDetail from './feedObjectDetail.js';
import Logged from "../../Logged";

<Logged><FeedObjectDetail/></Logged>

#### Usage

```jsx
 import React from 'react';
 import {SCContextProvider} from '@selfcommunity/react-core';
 import {FeedObjectDetail} from '@selfcommunity/react-templates';

 function App() {
   const conf = {
     portal: '<community-path>',
     session: {
       type: 'OAuth',
       token: '<community-token>',
       refreshToken: '<community-refresh-token>',
       refreshTokenEndpoint: '<community-refresh-endpoint>',
     },
   };
   return (
     <SCContextProvider conf={conf}><FeedObjectDetail/></SCContextProvider>
   );
 }
 ```