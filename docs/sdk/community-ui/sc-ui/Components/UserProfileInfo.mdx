---
sidebar_label: User Profile Info
sidebar_position: 1
title: User Profile Info
---

> The User Profile Info component renders the user profile info.

import UserProfileInfo from './userProfileInfo.js';
import Logged from "../../Logged";

<Logged><UserProfileInfo/></Logged>


#### Usage

```jsx
 import React from 'react';
 import {SCContextProvider} from '@selfcommunity/core';
 import {UserProfileInfo} from '@selfcommunity/ui';

 function App() {
   const conf = {
     portal: '<community-path>',
     session: {
       type: 'OAuth',
       token: '<community-token>',
       refreshToken: '<community-refresh-token>',
       refreshTokenEndpoint: '<community-refresh-endpoint>',
     },
   };
   return (
     <SCContextProvider conf={conf}><UserProfileInfo /></SCContextProvider>
   );
 }
```