---
sidebar_label: Account Sign In
sidebar_position: 1
title: Account Sign In
---

> The Account Sign In component allows users to log in to the application with their usernames and passwords.<br/>
This component is composed of a simple email and password form and a "sign in" button that submits the request.

import Anonymous from '../../Anonymous.js';
import AccountSignIn from './accountSignIn.js';

<Anonymous><AccountSignIn/></Anonymous>

#### Usage

```jsx
 import React from 'react';
 import {SCContextProvider} from '@selfcommunity/core';
 import {AccountSignIn} from '@selfcommunity/ui';

 function App() {
   const conf = {
     portal: '<community-path>',
     session: {
       type: 'OAuth',
       token: '<community-token>',
       refreshToken: '<community-refresh-token>',
       refreshTokenEndpoint: '<community-refresh-endpoint>',
     },
   };
   return (
     <SCContextProvider conf={conf}><AccountSignIn /></SCContextProvider>
   );
 }
```